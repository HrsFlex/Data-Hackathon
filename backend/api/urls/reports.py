from django.urls import path

urlpatterns = [
    # Reports endpoints will be added here
    # path('generate/', ReportGenerationView.as_view(), name='report-generate'),
    # path('templates/', ReportTemplateListView.as_view(), name='report-templates'),
    # path('<uuid:pk>/download/', ReportDownloadView.as_view(), name='report-download'),
]